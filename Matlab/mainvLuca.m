function mainvLuca
numn = 10;
numc = 300000;
factor = 1;
threshold = 0.2;

start_all = tic;

%NON FARE CAZZATE BAGGIO, onn sovrascrivere mai l'histogram reale
%delayHistogram('01',numn,numc)
%media(numn, numc)
%NormalizeHist_vLuca(numn)
%CompressHist(numn, factor)
%edgesCalc_vLuca(threshold,numn)

t_all = toc(start_all)
